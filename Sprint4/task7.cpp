#include <iostream>
#include <vector>

int main() {
    int n; std::cin >> n;
    int first_conter, second_counter, result;
    std::vector<int>storage(n);
    for(int i =0;i != n;++i) {
        char winner;
        std::cin >> winner;
        storage.push_back(winner);
    }
    bool flag = 1;
    for(int i=0; i != n;++i) {
        
    }
    return 0;
}